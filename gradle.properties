# Asciidoctor-PDF + Mermaid-Rendering (asciidoctorj, JRuby) brauchen mehr
# Heap/Metaspace als die Gradle-Defaults (512m/384m) — sonst OOM beim Bauen
# vieler Diagramme.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m
